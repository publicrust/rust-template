using System;
using Facepunch.Models;

public class Auth
{
	public string Type;

	public string Id;

	public string Ticket;

	public static Auth Steam(byte[] ticket, ulong steamId)
	{
		Auth auth = new Auth();
		auth.Type = "steam";
		auth.Id = steamId.ToString();
		auth.Ticket = BitConverter.ToString(ticket).Replace("-", "");
		return auth;
	}
}
