using Apex.Serialization;
using Rust.Ai;

public sealed class TargetEntityVisibleFor : BaseScorer
{
	[ApexSerialization]
	public float duration;

	public override float GetScore(BaseContext c)
	{
		return (!(c.AIAgent.AttackTargetVisibleFor >= duration)) ? 0f : 1f;
	}
}
