using Rust.Ai;

public class HasChairTarget : BaseScorer
{
	public override float GetScore(BaseContext context)
	{
		return Test(context as NPCHumanContext);
	}

	public static float Test(NPCHumanContext c)
	{
		return (!(c.ChairTarget != null)) ? 0f : 1f;
	}
}
