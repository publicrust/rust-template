using UnityEngine;

public class FlashlightBeam : MonoBehaviour
{
	public Vector2 scrollDir;

	public Vector3 localEndPoint = new Vector3(0f, 0f, 2f);

	public LineRenderer beamRenderer;

	private void LateUpdate()
	{
		if ((bool)beamRenderer && (bool)beamRenderer)
		{
			float time = Time.time;
			Vector3 position = base.transform.position;
			Vector3 vector = base.transform.localToWorldMatrix.MultiplyPoint(localEndPoint);
			beamRenderer.enabled = false;
		}
	}
}
