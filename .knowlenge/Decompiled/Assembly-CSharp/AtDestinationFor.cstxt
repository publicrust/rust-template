using Apex.Serialization;
using Rust.Ai;

public class AtDestinationFor : BaseScorer
{
	[ApexSerialization]
	public float Duration = 5f;

	public override float GetScore(BaseContext c)
	{
		return (!(c.AIAgent.TimeAtDestination >= Duration)) ? 0f : 1f;
	}
}
