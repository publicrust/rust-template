using System;
using UnityEngine;

public abstract class ModelConditionTest : PrefabAttribute
{
	public abstract bool DoTest(GameObject obj);

	protected override Type GetIndexedType()
	{
		return typeof(ModelConditionTest);
	}
}
