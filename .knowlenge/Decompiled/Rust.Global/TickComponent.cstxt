using Facepunch;
using UnityEngine;

public class TickComponent : MonoBehaviour
{
	public static TickComponent Instance;

	public static void Init()
	{
		if (!(Instance != null))
		{
			GameObject gameObject = new GameObject("Tick Manager");
			gameObject.AddComponent<TickComponent>();
		}
	}

	private void OnEnable()
	{
		Object.DontDestroyOnLoad(base.gameObject);
		Instance = this;
	}

	private void Update()
	{
		Tick.OnFrame();
	}

	private void LateUpdate()
	{
		Tick.OnFrameLate();
	}
}
