using Rust;
using UnityEngine;

[ExecuteInEditMode]
public class PropRendererDebug : MonoBehaviour
{
	public void Update()
	{
		PropRenderer[] array = Object.FindObjectsOfType<PropRenderer>();
		foreach (PropRenderer propRenderer in array)
		{
			propRenderer.DebugAlign();
		}
	}
}
