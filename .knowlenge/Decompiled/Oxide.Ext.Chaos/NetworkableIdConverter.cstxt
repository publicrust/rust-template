using System;
using Newtonsoft.Json;

public class NetworkableIdConverter : JsonConverter
{
	public override void WriteJson(JsonWriter writer, object value, JsonSerializer serializer)
	{
		writer.WriteValue(((NetworkableId)value).Value);
	}

	public override object ReadJson(JsonReader reader, Type objectType, object existingValue, JsonSerializer serializer)
	{
		if (reader.ValueType == typeof(ulong))
		{
			return new NetworkableId(serializer.Deserialize<ulong>(reader));
		}
		return default(NetworkableId);
	}

	public override bool CanConvert(Type objectType)
	{
		return objectType == typeof(NetworkableId);
	}
}
