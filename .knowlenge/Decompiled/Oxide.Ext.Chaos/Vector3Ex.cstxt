using UnityEngine;

public static class Vector3Ex
{
	public static float InverseLerp(Vector3 a, Vector3 b, Vector3 value)
	{
		Vector3 vector = b - a;
		return Vector3.Dot(value - a, vector) / Vector3.Dot(vector, vector);
	}

	public static float DistanceToLine(Vector3 a, Vector3 b, Vector3 c)
	{
		Vector3 vector = a - b;
		while (true)
		{
			int num = 363339358;
			while (true)
			{
				uint num2;
				switch ((num2 = (uint)(num ^ 0x2AC96FFD)) % 3)
				{
				case 2u:
					break;
				case 1u:
					goto IL_002a;
				default:
					return vector.magnitude;
				}
				break;
				IL_002a:
				vector = Vector3.Cross(vector.normalized, c - a);
				num = ((int)num2 * -618183743) ^ 0x164CEA47;
			}
		}
	}
}
