using System.Collections.Generic;
using Oxide.Ext.Chaos.AStar;
using UnityEngine;

public struct PathFailedResult
{
	public enum Enum
	{
		Generic,
		Unreachable,
		SmallerThanGrid,
		NotEnoughNodes
	}

	public object Reference;

	public Enum FailType;

	public List<Vector3> Results;

	public PathFailedResult(object reference, Enum failType, List<Vector3> results)
	{
		Reference = reference;
		FailType = failType;
		Results = results;
	}
}
