using System;
using Newtonsoft.Json;
using Unity.Mathematics;

public class float4Converter : JsonConverter
{
	public override void WriteJson(JsonWriter writer, object value, JsonSerializer serializer)
	{
		float4 @float = (float4)value;
		writer.WriteValue($"{@float.x} {@float.y} {@float.z} {@float.w}");
	}

	public override object ReadJson(JsonReader reader, Type objectType, object existingValue, JsonSerializer serializer)
	{
		if (reader.TokenType == JsonToken.String)
		{
			string[] array = reader.Value.ToString().Trim().Split(new char[1] { ' ' });
			return new float4(Convert.ToSingle(array[0]), Convert.ToSingle(array[1]), Convert.ToSingle(array[2]), Convert.ToSingle(array[3]));
		}
		return float4.zero;
	}

	public override bool CanConvert(Type objectType)
	{
		return objectType == typeof(float4);
	}
}
