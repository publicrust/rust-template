using Oxide.Ext.Discord.Builders;
using Oxide.Ext.Discord.Interfaces;

public class ScheduledEventLookup : IDiscordQueryString
{
	public bool? WithUserCount { get; set; }

	public string ToQueryString()
	{
		QueryStringBuilder queryStringBuilder = default(QueryStringBuilder);
		if (WithUserCount.HasValue)
		{
			queryStringBuilder.Add("with_user_count", WithUserCount.Value.ToString());
		}
		return queryStringBuilder.ToString();
	}
}
